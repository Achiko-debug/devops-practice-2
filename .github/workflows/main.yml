name: First DevOps Automation

on:
  push:
    branches:
      - main

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v3
      - name: Say Hello
        run: echo "გამარჯობა DevOps სამყაროში!"
